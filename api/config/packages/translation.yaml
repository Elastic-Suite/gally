framework:
    default_locale: en
    set_locale_from_accept_language: true
    enabled_locales: ['en', 'en_US', 'fr', 'fr_FR']
    translator:
        default_path: '%kernel.project_dir%/translations'
        fallbacks:
            - en
#        providers:
#            crowdin:
#                dsn: '%env(CROWDIN_DSN)%'
#            loco:
#                dsn: '%env(LOCO_DSN)%'
#            lokalise:
#                dsn: '%env(LOKALISE_DSN)%'
