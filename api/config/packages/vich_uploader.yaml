vich_uploader:
    db_driver: orm

    mappings:
        job_import:
            uri_prefix: /job/import
            upload_destination: '%kernel.project_dir%/var/jobs/import'
            namer: Vich\UploaderBundle\Naming\SmartUniqueNamer

