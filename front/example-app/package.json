{
  "name": "example-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "node build.js",
    "preview": "node server.js",
    "test": "echo \"[example-app] no tests yet\"",
    "test:ci": "echo \"[example-app] no tests yet\"",
    "eslint": "eslint --fix --ignore-path .gitignore .",
    "eslint:ci": "eslint --ignore-path .gitignore .",
    "prettier": "prettier --write --ignore-unknown --ignore-path .gitignore .",
    "prettier:ci": "prettier --check --ignore-unknown --ignore-path .gitignore .",
    "typescript": "echo \"[example-app] vanilla JS, no type check\"",
    "typescript:ci": "echo \"[example-app] vanilla JS, no type check\""
  },
  "browserslist": {
    "production": [">0.2%", "not dead", "not op_mini all"],
    "development": ["last 1 chrome version", "last 1 firefox version", "last 1 safari version"]
  },
  "devDependencies": {
    "eslint": "8.23.1",
    "eslint-config-prettier": "8.5.0",
    "prettier": "2.7.1"
  }
}
