# Components
- Reuse these primitives; do NOT create new visual primitives.
- No hardcoded hex/px/font-size — use tokens from `../../docs/design-system.md`.
- Never change a component's public props without checking all call sites in `../pages`.