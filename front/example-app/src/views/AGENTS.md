# Pages
- Each page has a spec in `../../specs/feature-*.md` — read it before editing.
- Every page MUST fire its tracking events (see `../../docs/sdk-reference.md`).
- Layout/styling: tokens only, per `../../docs/design-system.md`.